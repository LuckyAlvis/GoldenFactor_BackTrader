# 特斯拉（TSLA）股票数据获取工具

## 📋 项目说明

本工具用于获取美股特斯拉（TSLA）的实时和历史数据，并保存到CSV文件中。支持多种数据类型和时间周期。

## 📁 文件说明

### 1. fetch_tesla_simple.py（推荐新手）
**简化版，直接运行即可**

```bash
python3 fetch_tesla_simple.py
```

**功能**：
- 获取最近1年的日线数据
- 自动保存到 `tsla_data.csv`
- 显示统计信息和实时价格

**输出数据列**：
- Symbol: 股票代码
- Date: 日期
- Open: 开盘价
- High: 最高价
- Low: 最低价
- Close: 收盘价
- Volume: 成交量
- Dividends: 股息
- Stock Splits: 股票拆分

### 2. fetch_tesla_data.py（完整版）
**功能丰富，支持多种数据获取方式**

```bash
python3 fetch_tesla_data.py
```

**功能菜单**：
1. 获取历史数据（日线，默认1年）
2. 获取历史数据（自定义周期和间隔）
3. 获取实时报价（当前价格）
4. 获取日内分钟数据（最近7天）
5. 连续获取实时数据（可设置间隔和时长）
6. 获取所有类型数据

## 🚀 快速开始

### 步骤1：安装依赖

```bash
pip3 install yfinance
```

### 步骤2：运行脚本

**方式A：简单快速（推荐）**
```bash
python3 fetch_tesla_simple.py
```

**方式B：完整功能**
```bash
python3 fetch_tesla_data.py
```

### 步骤3：查看数据

数据会自动保存到CSV文件中，可以用Excel、Python或其他工具打开。

## 📊 数据获取示例

### 示例1：获取1年日线数据

```python
import yfinance as yf

tsla = yf.Ticker("TSLA")
df = tsla.history(period='1y', interval='1d')
df.to_csv('tsla_daily.csv', index=False)
```

### 示例2：获取实时价格

```python
import yfinance as yf

tsla = yf.Ticker("TSLA")
info = tsla.info

print(f"当前价格: ${info['currentPrice']}")
print(f"市值: ${info['marketCap']:,.0f}")
```

### 示例3：获取5分钟数据

```python
import yfinance as yf

tsla = yf.Ticker("TSLA")
df = tsla.history(period='7d', interval='5m')
df.to_csv('tsla_5min.csv')
```

### 示例4：连续获取实时数据

```python
import yfinance as yf
import time

tsla = yf.Ticker("TSLA")

for i in range(10):  # 获取10次
    df = tsla.history(period='1d', interval='1m')
    latest = df.iloc[-1]
    print(f"价格: ${latest['Close']:.2f}")
    time.sleep(60)  # 等待60秒
```

## 📈 支持的时间周期

### period（时间周期）
- `1d` - 1天
- `5d` - 5天
- `1mo` - 1个月
- `3mo` - 3个月
- `6mo` - 6个月
- `1y` - 1年（默认）
- `2y` - 2年
- `5y` - 5年
- `10y` - 10年
- `ytd` - 年初至今
- `max` - 所有历史数据

### interval（数据间隔）
- `1m` - 1分钟（仅限最近7天）
- `2m` - 2分钟
- `5m` - 5分钟
- `15m` - 15分钟
- `30m` - 30分钟
- `60m` - 60分钟
- `90m` - 90分钟
- `1h` - 1小时
- `1d` - 1天（默认）
- `5d` - 5天
- `1wk` - 1周
- `1mo` - 1月
- `3mo` - 3月

## 🔍 数据字段说明

| 字段 | 说明 | 单位 |
|------|------|------|
| Date | 日期时间 | - |
| Open | 开盘价 | 美元 |
| High | 最高价 | 美元 |
| Low | 最低价 | 美元 |
| Close | 收盘价 | 美元 |
| Volume | 成交量 | 股 |
| Dividends | 股息 | 美元 |
| Stock Splits | 股票拆分比例 | - |

## 💡 使用场景

### 1. 量化交易回测
```python
# 获取历史数据用于回测
import backtrader as bt
import pandas as pd

df = pd.read_csv('tsla_data.csv')
data = bt.feeds.PandasData(dataname=df)
```

### 2. 技术分析
```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_csv('tsla_data.csv')

# 计算移动平均线
df['MA20'] = df['Close'].rolling(window=20).mean()
df['MA60'] = df['Close'].rolling(window=60).mean()

# 绘图
df[['Close', 'MA20', 'MA60']].plot()
plt.show()
```

### 3. 实时监控
```python
# 每分钟获取最新价格
import yfinance as yf
import time

tsla = yf.Ticker("TSLA")

while True:
    df = tsla.history(period='1d', interval='1m')
    price = df.iloc[-1]['Close']
    print(f"当前价格: ${price:.2f}")
    time.sleep(60)
```

### 4. 数据分析
```python
import pandas as pd

df = pd.read_csv('tsla_data.csv')

# 计算收益率
df['Returns'] = df['Close'].pct_change()

# 计算波动率
volatility = df['Returns'].std() * (252 ** 0.5)
print(f"年化波动率: {volatility:.2%}")
```

## ⚠️ 注意事项

### 1. 数据限制
- **分钟数据**：yfinance只提供最近7天的分钟级数据
- **实时数据**：有15-20分钟延迟（非专业版）
- **请求频率**：建议不要过于频繁请求，避免被限制

### 2. 时区问题
- 数据时间为美国东部时间（EST/EDT）
- 需要根据需要转换为本地时区

### 3. 数据质量
- 免费数据可能存在缺失或延迟
- 重要交易决策建议使用付费数据源

### 4. 网络要求
- 需要稳定的网络连接
- 可能需要科学上网（取决于地区）

## 🛠️ 常见问题

### Q1: 安装yfinance失败？
```bash
# 尝试升级pip
pip3 install --upgrade pip

# 重新安装
pip3 install yfinance
```

### Q2: 获取数据失败？
- 检查网络连接
- 确认股票代码正确（TSLA）
- 尝试减少数据量（缩短时间周期）

### Q3: 数据为空？
- 可能是非交易时间
- 检查时间周期和间隔是否合理
- 尝试使用 `period='max'` 获取所有数据

### Q4: 如何获取其他股票数据？
```python
# 只需修改股票代码
aapl = yf.Ticker("AAPL")  # 苹果
msft = yf.Ticker("MSFT")  # 微软
googl = yf.Ticker("GOOGL")  # 谷歌
```

### Q5: 如何获取多只股票数据？
```python
import yfinance as yf

# 一次获取多只股票
tickers = yf.Tickers("TSLA AAPL MSFT")

# 分别获取数据
tsla_data = tickers.tickers['TSLA'].history(period='1y')
aapl_data = tickers.tickers['AAPL'].history(period='1y')
```

## 📚 进阶功能

### 1. 获取财务数据
```python
import yfinance as yf

tsla = yf.Ticker("TSLA")

# 财务报表
print(tsla.financials)
print(tsla.balance_sheet)
print(tsla.cashflow)

# 基本信息
print(tsla.info)
```

### 2. 获取期权数据
```python
import yfinance as yf

tsla = yf.Ticker("TSLA")

# 期权到期日
print(tsla.options)

# 获取特定到期日的期权链
opt = tsla.option_chain('2025-12-19')
print(opt.calls)  # 看涨期权
print(opt.puts)   # 看跌期权
```

### 3. 获取新闻
```python
import yfinance as yf

tsla = yf.Ticker("TSLA")

# 最新新闻
news = tsla.news
for item in news[:5]:
    print(f"{item['title']}")
    print(f"{item['link']}\n")
```

## 📊 数据可视化示例

### 使用matplotlib绘制K线图
```python
import pandas as pd
import matplotlib.pyplot as plt
from matplotlib.dates import DateFormatter
import matplotlib.dates as mdates

df = pd.read_csv('tsla_data.csv')
df['Date'] = pd.to_datetime(df['Date'])

fig, ax = plt.subplots(figsize=(12, 6))

# 绘制收盘价
ax.plot(df['Date'], df['Close'], label='Close Price')

# 设置标题和标签
ax.set_title('Tesla (TSLA) Stock Price', fontsize=16)
ax.set_xlabel('Date')
ax.set_ylabel('Price ($)')
ax.legend()
ax.grid(True)

plt.tight_layout()
plt.show()
```

## 🔗 相关资源

- **yfinance文档**: https://github.com/ranaroussi/yfinance
- **Yahoo Finance**: https://finance.yahoo.com/quote/TSLA
- **特斯拉投资者关系**: https://ir.tesla.com/
- **Backtrader文档**: https://www.backtrader.com/

## 📝 更新日志

### v1.0 (2025-11)
- ✅ 实现基础数据获取功能
- ✅ 支持历史数据和实时数据
- ✅ 提供简化版和完整版两个脚本
- ✅ 完善文档和示例

## 📧 技术支持

如有问题或建议，欢迎反馈！

---

**免责声明**：本工具仅供学习和研究使用，不构成投资建议。股市有风险，投资需谨慎。
